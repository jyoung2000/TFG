# Audit working notes (rebuild the final reports from this)

Snapshot: `hermes-review` @ 64bb6a3. Branch for the audit: `review/hermes`.
Host: Windows 11 build 26200, RTX 4070 12282 MiB, driver 616.64, Node 22.23.2, pnpm 10.30.3, uv 0.12.3.
Wan2GP checkout: 4fbc9827bad970be22d933470e0b7ffec31c99f9 (2026-09-22).

Format: `id | area | severity | reproduction | status | notes`

| id | area | severity | reproduction | status | notes |
|---|---|---|---|---|---|
| F-001 | setup / docs | low | fresh `git clone` + `git fetch origin`; `hermes-review` is not HEAD and is not listed by `git branch -a` | confirmed | The prompt's step-1 sequence is under-specified: you must `git fetch origin hermes-review` explicitly. Docs issue, not a product bug. |
| F-002 | gates | ok | `scripts/verify-hermes-ready.ps1` | PASS (29/29 ok, exit 0) | The readiness script itself is sound; every line says ok. |
| F-003 | gates | ok | `pnpm typecheck:ts` | PASS (exit 0) | tsc + devtools/ui-mock tsconfig. |
| F-004 | gates | ok | `pnpm test:frontend` | PASS 53/53, 9 files, 1.15 s | matches the session-notes Gate 9 claim. |
| F-005 | gates | ok | `pnpm build:frontend` | PASS | main chunk 308.16 kB; D-039 claimed 302 kB (close, not exact). Project 549.70 kB, ShotComposer 647.33 kB, react-vendor 203.45 kB. Vite warns >500 kB on 3 chunks. |
| F-006 | **setup / deps** | **blocker** | `pnpm setup:dev:win` then `backend\.venv\Scripts\python -m pytest -q tests` → `No module named pytest` | confirmed | **setup:dev:win does not install the test extra.** `scripts/setup-dev.ps1:49` runs `uv sync --extra dev`; `dev = ["pyright>=1.1.380","debugpy>=1.8"]` and `test = ["pytest>=8.0","requests>=2.31","httpx>=0.27"]` (backend/pyproject.toml:47-48). The documented gate in the audit prompt and docs/TESTING.md therefore cannot run after the documented setup. `pnpm backend:test` DOES pass `--extra test --extra dev`, so the two documented paths disagree. |
| F-007 | **setup / deps** | **blocker** | `pnpm setup:dev:win` then `backend\.venv\Scripts\pyright` → 10 errors | confirmed | **The documented setup downgrades the backend's own locked deps by installing Wan2GP's requirements into the same venv.** `scripts/ensure-wan2gp.ps1:101` runs `uv pip install --python $PythonExe -r Wan2GP/requirements.txt` against `backend\.venv\Scripts\python.exe` (`setup-dev.ps1:55`). Wan2GP pins `transformers==4.54.0`, `numpy==2.1.2`, `pydantic==2.11.10`, `diffusers==0.36.0`, `peft==0.17.0`; uv.lock pins `transformers 4.57.6`, `numpy 2.4.2`, `pydantic 2.12.5`, `diffusers` (git rev 01de02e8), `peft 0.18.1`, `tokenizers 0.22.2`. Setup silently wins and mutates the venv off-lock. All 10 pyright errors are the consequence (F-008/F-009). |
| F-008 | **backend / vision** | **blocker** | in the setup-produced venv: `from transformers import Florence2ForConditionalGeneration` → `ImportError` | confirmed | **The vision stack cannot load in the environment the documented setup produces.** `backend/services/vision/florence2.py:128` imports `Florence2ForConditionalGeneration`, absent in transformers 4.54.0 (arrives 4.56+; uv.lock pins 4.57.6 and session-notes VF-005 records verifying it there). Because `WanGPBridge._load_api` does `sys.path.insert(0, wan2gp_root)` + `importlib.import_module("shared.api")` **in-process** (`backend/services/wangp_bridge.py:364-366`), Wan2GP and the backend must share one venv — the pins are in direct conflict and one side must lose. Today Wan2GP always wins, so Florence2/CLIP/DINOv2/Depth lose. Architectural, not a typo: D-008 already moved vision to a sidecar (`backend/.venv-vision`, `scripts/ensure-vision.ps1`, `vision-requirements.txt` pins `transformers>=4.56,<5`) but D-008 also makes in-process the *default*, and the 4070 preset sets `vlm_provider=off` (D-009), so the default path on the target machine is exactly the broken one. |
| F-009 | gates / pyright | high (consequence of F-007) | `cd backend && .venv/Scripts/pyright` → 10 errors | confirmed | All 10 stem from the off-lock numpy/transformers: `floating[Unknown]`/`signedinteger` in `services/motion/flow_math.py:39`, `motion_analyzer.py:11`, `services/vision/depth.py:124`, `deterministic.py:64`, and `florence2.py:128` (`Florence2ForConditionalGeneration` is an unknown import symbol). Same 10 files fail on the *locked* versions for the same reason — the locks, not the code, are what moved. |
| F-010 | **setup / scripts** | **blocker** | `pnpm backend:dev:win` (= `scripts/start-backend.ps1`) → PowerShell `ParserError`, never starts | confirmed + FIXED | **The documented Windows headless-backend command does not run.** `scripts/start-backend.ps1` and `scripts/docker-desktop-repair.ps1` are UTF-8 *without* BOM and contain U+2192 (→). Windows PowerShell 5.1 (the `powershell` that `pnpm backend:dev:win` invokes) decodes BOM-less files as the OEM codepage (IBM437 here), so `→` (E2 86 92) becomes `0x92` = an apostrophe, which closes the string literal early and cascades into ~8 parse errors. Verified with `[Parser]::ParseFile` across all 10 `.ps1` in `scripts/`: exactly these 2 fail. **Fix:** added a UTF-8 BOM to both; all 10 now parse, and `start-backend.ps1` boots the backend. |
| F-011 | **training / guard** | **high** | `POST /api/training/runs` with `config={"target":"wan22","estimated_vram_mb":1,...}` → **HTTP 200, run completes** | confirmed + FIXED | **The 12 GB guard trusted a client-supplied number.** `TrainingConfig.estimated_vram_mb` is a *body field*; `fits_machine()` compared that client value against VRAM, so any hand-written config (MCP tool call, curl, stale UI payload) set it to 1 and a **24 GB `wan22` LoRA trained to completion on this 12 GB card** (reproduced live: a `wan22` run with 800 steps, 6 checkpoints and a registered LoRA, peak_vram_mb 1024). The UI path is safe (`/api/training/suggest` returns 24000/32768 for video targets) which is why the existing test `test_starting_a_video_lora_on_this_card_is_refused_before_any_work` passed — it posts `default_config(...)`, i.e. the honest number. **Fix:** `film/training_presets.py::fits_machine` now takes `max(client value, server table value for the target)`; the error text reports the server figure. Regression test `test_a_client_cannot_understate_the_vram_estimate_to_unlock_a_video_target` (proved red before the fix: `assert 200 == 400`). |
| F-012 | gates / licenses | medium | `pytest tests/test_licenses.py` after a local build → 407 AGPL hits | confirmed + FIXED | `python-embed/` (the `prepare-python` runtime, gitignored, 0 tracked files) was not in `SKIP_DIRS`, so the AGPL guard scanned ~28 MB of third-party wheels (cv2, ultralytics) and failed the build. `Wan2GP/` had the same problem. **Fix:** added both to `SKIP_DIRS` with the reasoning. Guard strength re-proved: planting a real AGPL header at `backend/_agpl_probe.py` still fails the test; removing it returns 4/4 pass. Not a weakened test. |
| F-013 | gates / motion | medium | `pytest tests/test_motion.py::TestRealAnalyzer` → `AttributeError: module 'cv2' has no attribute 'calcOpticalFlowFarneback'` | confirmed + FIXED (environment) | **Corrupted `opencv-python-headless` install, not a code bug.** Wan2GP's `requirements.txt` (installed into the same venv by `ensure-wan2gp.ps1:101`) left `site-packages/cv2/` holding only `data/`, `gapi/`, `ml/` and a videoio DLL — 37 of the dist's own files, including `cv2/__init__.py`, were missing, so `cv2` imported as an empty namespace package. **Fix:** `uv pip install --reinstall-package opencv-python-headless==4.13.0.92`; `cv2 4.13.0` with `calcOpticalFlowFarneback` present, `tests/test_motion.py` 12/12. Same shared-venv disease as F-007. |
| F-014 | gates / pyright | medium | `pytest tests/test_pyright.py` on a venv where pyright is not on PATH → `FileNotFoundError [WinError 2]` | confirmed + FIXED | The test's own "pyright not available → skip" branch was unreachable: `subprocess.run(["pyright", ...])` raises before any output exists, so the JSONDecodeError handler that calls `pytest.skip` never ran, and a missing *optional* tool became a hard failure. **Fix:** `_pyright_command()` resolves `pyright.exe` next to `sys.executable` first, then PATH, and skips only if neither exists. Side effect: the gate now actually runs and enforces pyright (it previously could not). |
| F-015 | **backend / vision** | **high** | `POST /api/vision/analyze` on a real image → `200` but `caption: null`, `regions: []`, `notes: {"caption": "BartTokenizerFast has no attribute image_token", ...}` | confirmed, NOT fixed | **Florence2 captioning and grounding silently produce nothing on this machine.** `Florence2Processor.__init__` (transformers 4.57.6, `models/florence2/processing_florence2.py:121`) reads `tokenizer.image_token`; the stock `BartTokenizerFast` for `microsoft/Florence-2-large` has no such attribute, and the cached `tokenizer_config.json` has only `model_max_length` (no `additional_special_tokens`, no `image_token`). `GET /api/vision/status` still reports the component as `"available": true` (availability is a config/spec check, not an import or attribute check), so the UI and Reproduce's evidence panel get an empty caption and zero regions with no error surfaced. The rest of the stack is genuinely good: CLIP tags real ("a minimalist painting", "minimalism", "Karl Gerstner") and Depth-Anything produced a real depth map. **Not fixed** because the fix is a transformers-version or model-asset decision I cannot prove here (no newer/older transformers verified against this processor, and changing the vision stack's model set is a product call). CLIP + depth carry U5/U6 today; caption/regions do not. |
| F-016 | training / UX | low | `POST /api/training/datasets/{id}/import` on a folder containing `clip-01.mp4` and `README.md` | confirmed, works | Non-image files are skipped correctly (3 of 5 files imported as items). The `.mp4`/`.txt` were not imported and no error was raised. A dataset folder with junk is safe. |
| F-017 | **runtime / deps** | **blocker** | after `uv sync` (the gate path), `POST /api/generate-image` → `500 {"error": "No module named 'gradio'"}`, then `"No module named 'mmgp'"` | confirmed, NOT fixed in-repo | **The two documented setups are mutually exclusive; generation is broken under the one the gates require.** `uv sync` prunes everything outside `backend/uv.lock`, but **81 of Wan2GP's 99 top-level requirements are not in that lock** (measured: apprise, gradio, mmgp, librosa, ultralytics, rembg, insightface, decord, timm, …). `WanGPBridge` imports `shared.api` in-process, so a render needs all of them. So: `pnpm setup:dev:win` (installs Wan2GP reqs) → generation works, gates broken (F-007/F-009); any `uv sync` → gates work, **generation dead on the first render**. I only got a render by re-running `uv pip install -r Wan2GP/requirements.txt` by hand. Not fixed: the real repair is to stop sharing one venv (give Wan2GP its own, as the trainers already get `backend/.venv-trainer-*` per D-028) or to lock the union — an architectural change I cannot prove safe in one run. |
| F-018 | vram / docs | medium | `POST /api/generate` (video) → `507 {"error": "Free 1.3 GB of VRAM before rendering: 8.5 GB free, 9.8 GB needed. Loaded: nothing this app owns."}` | confirmed, correct behaviour / stale doc | The guard is **right and honest**: `RENDER_NEEDS_MB["ltx2_22B_distilled"] = 9500` + `SAFETY_MARGIN_MB 512` = 9.8 GB, and `nvidia-smi` confirms only 8.95 GB free (3.3 GB held by the desktop compositor and other apps). Refusal-not-crash on the 12 GB card, with a number and a next step. **But** the comment at `services/vram/vram_manager.py:33` says the table is "overridable in settings", and it is not: `render_needs_mb` is a constructor parameter that **no caller ever passes** (`app_handler.py:182`, `vision_worker.py:78` both use defaults), so a user on a busy desktop has no way to lower the bar. The error even says "Loaded: nothing this app owns", i.e. it knows it cannot free anything itself. |
| F-019 | U1 image render | — | `POST /api/generate-image` "a rain-soaked neon alley, cinematic", 1024×1024, 8 steps | **MEASURED, works** | `200` in **226.7 s** (first run, includes model load). Output a real 1024×1024 RGB JPEG, 337 KB. Peak VRAM: History `metrics.peak_vram_mb` = **8044**, my independent `nvidia-smi` sampler = **7773** (the app samples post-hoc, as `docs/RTX_4070_TEST_MATRIX.md` honestly says). Visually inspected: coherent cyberpunk alley, wet reflective ground, neon signage, correct perspective — matches the prompt; only garbled sign lettering, the normal pseudo-text artifact. History recorded the prompt and seed 1790433998. **This is the feature working as advertised.** |
| F-020 | **U2/U3 video** | **blocker** | `POST /api/generate` (540p · 6 s) with the guard threshold temporarily lowered 9500→8000 | confirmed, NOT fixed | **Video generation cannot start on the reference machine, for two independent reasons.** (1) The VRAM bar is above what the card can ever give: `9500 + 512` = 9.8 GB needed, and `nvidia-smi` shows a hard ceiling of ~8.95 GB free (3.3 GB is held by the Windows compositor, Discord, Docker Desktop, PowerToys, browsers, etc.). The guard refuses with a correct, actionable 507 — but per F-018 there is no supported way to lower the number, so on a 12 GB card **video is unreachable** while images work fine at 7.5 GB. That is a straight contradiction of the README's headline ("reduces the VRAM requirements from 32 GB to 6 GB") and of the 4070 preset existing at all. (2) With the bar forced down, the job started and then began silently **downloading a 19.4 GB checkpoint** (`ltx-2.3-22b-distilled_diffusion_model_quanto_int8.safetensors`) — I killed it at 66% / ~8 min in. `GET /api/models/library` reports `ltx2_22B_distilled installed: false`, so the app *knows* the weights are absent, yet `/api/generate` accepted the request, created a `running` job, and began a multi-gigabyte download with no confirmation prompt, no size warning and no "this will take ~10 minutes and 19 GB" message. Contrast the image path, which checked weights up front (`video_generation_handler` raises "Models not downloaded" in the non-WanGP path). I could not measure a real video render, so **U2 and U3 stay unmeasured** and the RTX matrix rows stay BLOCKED with this reason. Guard restored to 9500 afterwards (`git diff` clean). |
| F-021 | History (U11) | — | two failed image renders, then `GET /api/jobs` | **MEASURED, works well** | Both failures landed as `status: failed` with the *real* error preserved (`"No module named 'gradio'"`, `"No module named 'mmgp'"`) plus the prompt and seed. No orphaned `running` job, no stuck state — the exact failure mode the audit brief asks about. Metrics were `{}` on failures, which is correct (nothing to measure). |
| F-022 | **U5 Reproduce** | **high** | `POST /api/reproduce/import` ×3 → `analyze` → `start {candidates:2, rounds:1, target_score:0.9}` (ran to 3 rounds, 18 candidates) | runs, but **misleading** | The loop works mechanically: 18 real 1024×576 candidates, 3 rounds, each scored on 4 components with `weights_used` and an explicit `missing: ["layout"]`. **But the scores are not trustworthy and the rounds do not improve.** Measured round means **r1 0.5874 → r2 0.5865 → r3 0.5959** (flat, within noise); best-of-round **0.6416 → 0.6094 → 0.6329** (not monotonic); the `target 0.9` is **never reached** and the run ends anyway. I put the reference, the app's best candidate (0.6416) and its worst (0.5510) side by side and looked at them: the ordering is *directionally* right, but **neither candidate resembles the reference at all** — the reference is a near-black minimalist room with a small figure and one yellow square; the "best" candidate is a pale beige abstract of folded triangles with no figure and no square. So a 0.64 composite is being reported for an obvious miss. |
| F-023 | U5 scoring | high | same run, `spec.provenance` / `spec.subjects` / `scores.weights_used` | confirmed, root cause of F-022 | **The score is high because the spec is empty, not because the match is good.** `provenance` reads `{measured: "measured", style: "clip", layout3d: "depth"}` — no `florence` — and `spec.subjects` is `[]` while `scene` is all empty strings, because Florence2 is dead (F-015). So the compiled prompt never says *what is in the picture*, and the remaining components are all coarse: CLIP 0.39 weight, DINOv2 0.28, SSIM 0.17, palette 0.17. Palette+SSIM are only 0.33 of the weight and both are satisfied by "brownish and similarly laid out", which is why every candidate clusters at 0.55–0.64. The app is **honest about the mechanics** (it reports `missing: ["layout"]` and per-component numbers) but presents a number that reads as "64% similar" when the honest answer is "not similar". This is the brief's "a high score on a poor match is a finding", and it is real. |
| F-024 | **U13 preset** | **high** | `POST /api/settings/presets/rtx-4070-12gb/apply` then `GET /api/settings` | applies, but **promises a broken feature** | The mechanism works: the preset applies, `hardwarePreset: "rtx-4070-12gb"`, `videoProfile: "fast"`, `imageSteps: 8`, `vision.vlmProvider: "off"` all persist, and `GET /api/settings/presets` reports `applied: true`. The problem is what it selects. Its `changes` list promises *"Vision: Florence-2-large captions, CLIP ViT-L/14 tags, Depth-Anything-V2-small, DINOv2-small"* and its profile notes promise *"a clip in about a minute on a 4070"*. Measured on this exact machine: **Florence-2-large returns no caption and no regions (F-015), and no clip is produced at all (F-020).** So the flagship "RTX 4070 · 12 GB" preset advertises exactly the two things that do not work here, and offers no warning. A preset is a promise; this one is not kept. |
| F-025 | U13 persistence | — | apply preset → restart backend → `GET /api/settings` | **works** | After a full backend restart the preset values are still there (`hardwarePreset: "rtx-4070-12gb"`, `videoProfile: "fast"`, `imageSteps: 8`, `vision.vlmProvider: "off"`). Settings genuinely persist. Note for anyone re-testing: the settings JSON is **camelCase** (`hardwarePreset`, `videoProfile`, `imageSteps`), while the docs and `api_types.py`-adjacent code speak snake_case (`hardware_preset`) in places — worth a consistency pass, though it is not itself a defect. |
| F-026 | Film Studio (U12) | — | create film → 2 characters + 1 location → scene → 3 shots → queue pause/resume | **works (structure)** | `POST /api/film/projects/{id}/assets` ×3 → 200 with ids; `/scenes` → 200; `/scenes/{id}/shots` ×3 → 200 with ids. `POST /api/film/queue/pause` → `{"status":"paused", queue:{...,"paused":true}}` and `/resume` → `{"status":"resumed","paused":false}` — the queue reports real state. I could not run the *render* half of U12 (queue previews → timeline) because it needs the video weights (F-020). |
| F-027 | 3D storyboard (U7) | medium | `POST /api/scene/build` with a real analysed ShotSpec | runs, **degraded** | The solver is real: returns a `layout3d` with a camera (`pos [0,1.6,0]`, `fov 40`), a `composition` object graph, a **real depth map path** from Depth-Anything, `camera_words` (`focal_mm 49.5`, "standard portrait perspective"), `camera_sentence`, `reprojection_error: 0.0` and a generated **SVG blockout**. But `objects: []` — the scene has no figures — because `spec.subjects` is empty (F-015 again). So the "move a figure in the composer" half of U7 has nothing to move on this machine. |
| F-028 | **BREAK-IT: two renders at once** | — | two concurrent `POST /api/generate-image` (768×768, 6 steps) | **works, correctly** | Render A → `200` in 18.9 s. Render B, fired 3 s later → **`409 {"error": "Generation already in progress"}`** in 0.0 s. No OOM, no crash, no corrupted output; the GPU was fine afterwards. This is the right behaviour and directly answers the brief's "two renders at once" case: the queue serialises instead of exploding. |
| F-029 | U1 steady-state | — | warm `POST /api/generate-image` 1024×1024, 8 steps (model already resident) | **MEASURED, meets target** | **30.0 s**, `peak_vram_mb` 5087 (768²) / 6524 (1024²). The brief's target was "< 30 s after warm-up" — met, just. Note the huge cold/warm gap: **226.7 s cold vs 30.0 s warm** (7.6×), essentially all model load. Other warm renders in the same session: 9.4 s, 9.6 s, 10.2 s at 768×768/6 steps. Visually inspected the warm 1024² output: coherent alley, correct one-point perspective, believable neon reflection in wet ground, no structural failures — only the usual garbled sign lettering. |
| F-030 | **MCP / safety docs** | **medium** | `hermes mcp test tfg`-style config from the docs, then look for the excluded tool | confirmed, NOT fixed | **The documented safety exclusion does not exist.** Both `docs/AGENTS_GUIDE.md` and `skills/tfg/SKILL.md` instruct the operator to set `tools: exclude: [system_shutdown]`, and both say "`system_shutdown` is a real tool". It is not: `tools/list` returns **no** `system_shutdown`. The actual tool is **`health_shutdown`** (from `POST /api/system/shutdown`, tagged `health`). So an agent that follows the documentation exactly leaves a tool that **stops the user's backend** enabled, and the exclusion silently matches nothing. I hit this myself: a read-only sweep that filtered on the substring `health` called `health_shutdown` and the backend exited. Rename the tool to `system_shutdown` (or fix both docs) — either way the current state is a footgun that reads as protection. |
| F-031 | MCP surface (U15) | — | `initialize`, `tools/list` (214), `tools/call` on valid args, unknown tool, bad method, missing required field, nonexistent id | **works well** | `initialize` returns protocol `2025-06-18`, `serverInfo {name: "tfg", version: "1.0.0"}` and useful `instructions`. **`tools/list` returns exactly 214 tools**, matching the documented count and the 214 API operations. Unknown tool → `-32602 Unknown tool`; bad method → `-32601 Method not found`; missing required body field → `isError: true` carrying FastAPI's own validation text; `jobs_get_job` with a bad id → `isError: true, {"error": "Unknown job: nope"}`. **`/mcp` sits behind the same auth as the API** (open on loopback unless `LTX_AUTH_TOKEN` is set), and file routes return metadata rather than bytes, as documented. Error text is genuinely good — this is the strongest part of the codebase. |
| F-032 | **e2e / infra** | **blocker** | `pnpm e2e` on Windows → `Error: Timed out waiting 120000ms from config.webServer` | confirmed + FIXED | Two independent Windows-only causes, both invisible on the Linux container this was built on. (1) `playwright.config.ts` polled `http://127.0.0.1:5173` (IPv4) while Vite's default dev host `localhost` binds **`[::1]` only** on Windows — `netstat` showed `TCP [::1]:5173 LISTENING` while an IPv4 poll still refused. (2) The command was `pnpm dev:ui -- --port N --strictPort --host 127.0.0.1`; pnpm forwards the bare `--` to vite, which reads it as end-of-options, so **every flag was silently discarded** and the server always came up on 5173 regardless of `E2E_PORT`. **Fix:** poll `localhost`, and drop the `--` separator. Now **28 passed (2.0 m)** — matching the claimed 28/28 baseline, verified on real hardware. A third cause sat underneath: Vite's dep scanner descended into `python-embed/` and tried to pre-bundle gradio's bundled Svelte app, failing on unresolvable imports (fixed in the same commit via `optimizeDeps.entries` + `server.fs.deny`). |
| F-033 | **agent tooling** | high | `echo '{...initialize...}' \| pnpm agent:mcp` → `ModuleNotFoundError: No module named 'torch'` | confirmed + FIXED | **`pnpm agent:mcp` could never work.** The script was `python backend/tfg_mcp.py`, i.e. bare `python` from PATH — on this machine Python 3.11.15 with none of the backend's dependencies, while the project pins 3.12 and keeps everything in `backend/.venv`. `AGENTS_GUIDE.md` even claims "`pnpm agent:mcp` runs the stdio server with the repo's Python"; it did not. **Fix:** run it through `uv run` inside `backend/` with the env scrubbed, matching the existing `backend:test` script. Verified: `pnpm agent:mcp` now prints `TFG MCP: 214 tools → http://127.0.0.1:8000` and answers `initialize` over stdio. (Note the MCP *client* config in the docs already points at `backend/.venv/Scripts/python.exe`, which is why the stdio transport itself worked when I tested it directly — only the convenience script was broken.) |
